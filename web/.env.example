# Base URL of the FastAPI backend (no trailing slash).
# Baked in at build time (NEXT_PUBLIC_*), so set it in Vercel's project settings
# for deployments. When unset, the app defaults to http://localhost:8000.
NEXT_PUBLIC_API_URL=http://localhost:8000
